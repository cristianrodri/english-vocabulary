extends /includes/template.pug

append config
  -pageTitle = 'In, On, At'

block main
  include /includes/table.pug
    +word('En (dentro)', 'In')
    +word('En, sobre, encima', 'On')
    +word('En el, en la', 'At')